#Prompt for OpenAI API
PROMPT="Scrape the following companies' websites separately for each keyword and list new job positions posted this week.Companies:"

# Once you add your API key below in the quotes, make sure to not share it with anyone! The API key should remain private.
OPENAI_API_KEY=""